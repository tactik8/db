# Your Fastify App
  app:
    build:
      context: .
      
    ports:
      - "13001:3000"
    environment:
      # Use the 'mongodb' service name as the hostname
      - MONGODB_URL=mongodb://mongodb:27017/mydatabase
      - NODE_ENV=production
      - DATABASE_URI=mongodb://USERID:PASSWORD@SERVER:PORT/?authMechanism=DEFAULT
      - DATABASE_ID=n8n
      - TENANT_ID=test
    